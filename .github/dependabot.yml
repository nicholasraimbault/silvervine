version: 2
updates:
  # Rust dependencies (workspace root).
  - package-ecosystem: "cargo"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "08:00"
      timezone: "America/Los_Angeles"
    open-pull-requests-limit: 5
    labels:
      - "dependencies"
      - "rust"
    commit-message:
      prefix: "deps"
      include: "scope"
    groups:
      # Group all patch + minor updates of clap into one PR
      # (avoids PR-per-clap-subcrate noise).
      clap:
        patterns:
          - "clap"
          - "clap_*"
      # Group serde family.
      serde:
        patterns:
          - "serde"
          - "serde_*"
      # Group tracing family.
      tracing:
        patterns:
          - "tracing"
          - "tracing-*"
    # Ignore updates that require a coordinated API migration. Dependabot
    # should not turn a major sysinfo redesign into an unattended lockfile PR.
    ignore:
      - dependency-name: "sysinfo"

  # GitHub Actions (workflow YAML pinning).
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "08:00"
      timezone: "America/Los_Angeles"
    open-pull-requests-limit: 5
    labels:
      - "dependencies"
      - "github-actions"
    commit-message:
      prefix: "ci"
      include: "scope"
    # `dtolnay/rust-toolchain`'s `@<version>` ref pins a *Rust toolchain
    # version* (e.g. `@1.88`), not an action release. Dependabot reads
    # `@1.88` as a semver-ish pin and "bumps" it forward — but the
    # bumped value (e.g. `1.100`) is a non-existent Rust release, which
    # breaks MSRV CI on every subsequent PR. Ignore entirely; the
    # toolchain version is bumped manually alongside Cargo.toml's
    # `rust-version`.
    ignore:
      - dependency-name: "dtolnay/rust-toolchain"
